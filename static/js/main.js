//加载配置require.config({	baseUrl:'/static/js',	paths:{		layer:'layer',	    nprogress:'nprogress',		html:'//apps.bdimg.com/libs/html5shiv/r29/html5.min'	}});require(['layer','nprogress','html'],function(L,N){		$('body').show();	N.start();	setTimeout(function() { N.done(); $('body').removeClass('out'); }, 500);    $('#nav').on('click', function(){	  L.msg('的确很重要', {icon: 1});    });	$('.nav-menu').on("click", "a",function(){		 $(".nav-menu a").eq($(this).index()).addClass("b").siblings().removeClass('b');	});		$('.menu a').click(function(){			$(".menu a").eq($(this).index()).addClass("n").siblings().removeClass('n');		//父菜单数	    t =$(this).data("type");	    n =$(this).data("num");		url =$(this).data('href');		//console.log(n.length);        if(n > 0){            if(url.length == 0) {L.msg('所属链接不存在');return false;}            if(t == 0) {L.msg('所属链接不存在');return false;}	        $.ajax({	        type: "GET",	        url: "/common/json",	        data: {num:n,type:$(this).data("type")},		    dataType: "json",            success: function(data){                         $('.nav-menu').empty();                         var html = ''; 						  $('.nav-menu').html(html);					     if(data.error){html="";}else{                         $.each(data, function(commentIndex, comment){							/*$('<a data-href="'+comment['action']+'?id='+comment['id']+'" data-num="'+comment['id']+'" data-type="'+comment['typec']+'" target="main">' + comment['name'] + '</a>').appendTo('.nav-menu');*/                            $("<a href="+comment['action']+"/"+comment['typec']+"/"+comment['id']+" target='main'>"+comment['name']+"</a>").appendTo('.nav-menu');                         });				         }						 $('.nav-menu a:first').attr("class","b");				         //子菜单数						 link=$('.nav-menu a:first').attr('href');						 $("#iframepage").attr("src",link);                      }					  	          });			  		  }else{			  $("#iframepage").attr("src",url);	      }		    	});		/*    $('#list').on('click','a',function(){		alert(1);         parent.layer.open({            type: 2,			title: $(this).data('title'),            area: ['780px', '450px'],            fix: false, //不固定            maxmin: true,            content: $(this).data('href')         });    });	$('#list tbody tr').mouseover(function(){		 $("#list tbody tr").eq($(this).index()).addClass("on").siblings().removeClass('on');	});		$('#date-start').datetimepicker({       mask:''    });	$('#date-end').datetimepicker({       mask:''    });	$('#list tbody tr').mouseover(function(){		 $("#list tbody tr").eq($(this).index()).addClass("on").siblings().removeClass('on');	});	*/});